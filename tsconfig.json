{
  "compilerOptions": {
    "target": "es5",                     // 编译目标为 ES5，可以根据需要调整
    "module": "commonjs",                // 模块系统为 CommonJS，适合 Node.js 环境
    "strict": true,                      // 开启严格模式
    "esModuleInterop": true,             // 支持 ES 模块的导入形式
    "skipLibCheck": true,                // 跳过声明文件的检查，可以提高编译速度
    "forceConsistentCasingInFileNames": true,
    "jsx": "react",                      // 支持 React JSX 语法
    "outDir": "./dist",                  // 输出目录，确保与下面的 declarationDir 一致
    "declaration": true,                 // 生成声明文件
    "declarationDir": "./dist",          // 声明文件输出目录
    "moduleResolution": "node",          // 模块解析策略，使用 Node.js 解析
    "resolveJsonModule": true
  },
  "include": [
    "src/**/*"                          // 包含的源文件目录
  ],
  "exclude": [
    "node_modules",                     // 排除的目录
    "dist"                              // 排除生成的输出目录
  ]
}
